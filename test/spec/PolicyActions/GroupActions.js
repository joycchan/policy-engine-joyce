'use strict';

describe('Group Actions', function () {

  var PolicyActions;

  beforeEach(module('policyEngine'));

  beforeEach(inject(function(_PolicyActions_) {
    PolicyActions = _PolicyActions_;
  }));

  it('has a receive method', function() {
  });


});
